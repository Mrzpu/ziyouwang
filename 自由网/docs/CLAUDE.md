# 自由网精选酒店平台 · 开发文档

> 最后更新：2026年5月
> 当前阶段：前端静态原型，所有数据写死，待接入后端

---

## 一、项目概览

| 项目 | 说明 |
|------|------|
| 品牌名 | 自由网 · 精选酒店 |
| 商业模式 | 精选筛选 + 批发价格 + 会员制（¥299/年）|
| 起步城市 | 威海，山东省 |
| 扩张路径 | 省市两级，省级负责人制度 |
| 竞争差异 | 不靠佣金排名，比携程便宜15-25%，融合小红书体验标签 |

---

## 二、当前文件结构

```
ziyouwang_v3.html    主页（首页）
search.html          搜索结果页
hotel_detail.html    酒店详情页（仅君季L·DESIGN）
CLAUDE.md            本文档
```

---

## 三、已完成功能 ✅

### 主页 ziyouwang_v3.html
- [x] 导航栏：Logo + 搜索框 + 精选酒店/预订流程/会员权益 + 账户下拉菜单
- [x] 右侧每日场景面板：大号日期 + 年月星期 + 地点描述（飞猪风格）
- [x] 搜索卡片：省份下拉（省市两级联动）→ 城市 → 入住/退房（自研日历组件）→ 房间及住客（增减弹窗）→ 搜索按钮
- [x] 快捷标签：威海海景/五星精选/会员特价/含早餐/免费停车
- [x] 为你推荐酒店卡片（城市Tab切换）
- [x] 热门城市5宫格
- [x] 会员横幅
- [x] 预订四步流程
- [x] 页脚
- [x] goSearch()：点击搜索携带所有参数（省份/城市/入住/退房/人数）到search.html

### 搜索页 search.html
- [x] 导航栏：与主页一致
- [x] 顶部搜索栏：省份+城市+入住退房（合并，中间显示晚数）+房间及住客+搜索按钮
- [x] 胶囊筛选条：价格输入 + 星级（五/四/三星）+ 特色（海景/早餐/停车/泳池）+ 会员特价 + 应用筛选/重置
- [x] 8家威海酒店数据（真实数据）
- [x] 筛选/排序真实交互（价格区间/星级/特色/会员/推荐/评分）
- [x] 筛选后顶部显示active标签，可单独删除
- [x] 右侧地图面板（sticky）：价格Pin标注，点击酒店卡片高亮Pin + 底部信息
- [x] loadFromURL()：读取主页传来的URL参数自动填入
- [x] 点击酒店卡片携带参数跳转detail页
- [x] 地图面板：`AMAP_KEY = 'YOUR_AMAP_KEY'` 待填入，Key未填时显示友好提示

### 酒店详情页 hotel_detail.html（君季L·DESIGN酒店）
- [x] Swiper.js图片轮播：主轮播 + 缩略图联动 + 自定义箭头
- [x] 灯箱全屏浏览：点击任意图片打开，ESC关闭，底部缩略图联动
- [x] 真实信息：地址/评分/房型/价格/评价/周边全部填入
- [x] 酒店特色图标条（携程风格）：6个图标横排
- [x] 酒店设施4×2网格
- [x] 周边信息：交通/景点/美食三栏带距离
- [x] 评分总览：分项进度条（卫生/设施/环境/服务）
- [x] 评价标签可点击（样式）
- [x] 3条真实携程评价
- [x] 预订卡：房型下拉 + 自定义日历 + 人数增减弹窗
- [x] loadFromURL()：读取search.html传来的参数自动填入
- [x] 日历/人数弹窗：position:fixed + scroll/resize监听实时更新位置

### 页面间数据传递（全链路打通）
- [x] 主页 → 搜索页 → 详情页，日期+人数+城市参数全程保留
- [x] URL参数：checkin/checkout/rooms/adults/children/province/city
- [x] 本地时区日期解析（避免UTC偏移bug）

---

## 四、未完成 ❌

### 酒店详情页（还差3家）
- [ ] 威海心海海景酒店（国际海水浴场火炬八街店）
  - 评分4.9 / 725条 / 海景高楼层
  - **待用户提供携程截图**
- [ ] 汉庭威海火车站酒店
  - 评分5.0 / 1028条 / 性价比 / 近火车站
  - **待用户提供携程截图**
- [ ] 如家·neo（威海高铁北站张村政府店）
  - 评分4.9 / 228条 / 近高铁北站
  - **待用户提供携程截图**

### 搜索页
- [ ] 高德地图API接入（Key已申请中）
  - 位置：`search.html` 第一行 `const AMAP_KEY = 'YOUR_AMAP_KEY'`
  - 接入后：真实威海地图底图 + 8个酒店真实坐标Pin
- [ ] 搜索页酒店卡片数据对应正确的detail页
  - 现在全部指向 `hotel_detail.html`（君季）
  - 待其他3个detail页做完后，分别指向各自的文件

### 主页
- [ ] 右侧每日场景面板背景换成真实中国美景图片（现为渐变色占位）
- [ ] 为你推荐板块数据对应真实hotel detail页
- [ ] 热门城市卡片换成真实城市图片

### 所有页面
- [ ] 酒店图片：所有emoji占位需替换成真实图片
  - 替换方法：把 `<div class="slide-placeholder">🏨</div>` 换成 `<img src="图片路径">`
- [ ] 会员注册/登录页面
- [ ] 订单确认页面

---

## 五、未来要改进 🔧

### 第二阶段：数据动态化

**现在的问题：** 所有酒店数据写死在HTML里，改价格要动代码

**解决方案：** 接入 Supabase（免费托管数据库）

```javascript
// 现在（写死）
const HOTELS = [{id:1, name:'君季...', price:88}]

// 未来（动态）
const res = await fetch('https://xxx.supabase.co/rest/v1/hotels?city=威海')
const HOTELS = await res.json()
```

**需要做的：**
- [ ] 建 Supabase 数据库（hotels/rooms/reviews 三张表）
- [ ] 把8家酒店数据迁移进数据库
- [ ] 前端改为 fetch() 读取
- [ ] 价格/库存实时更新无需改代码

### 第三阶段：后台CMS系统

**逻辑：** 酒店方登录后台 → 填写/上传：房型、价格、图片、描述 → 提交审核 → 平台审核通过 → 前端自动更新

**技术栈：**
- 数据库：Supabase（MySQL）
- 后端API：Node.js 或 Python FastAPI
- 酒店方后台：独立管理界面
- 审核机制：状态字段 pending/approved/rejected

**需要做的：**
- [ ] 酒店方提交表单页面
- [ ] 审核后台界面（平台管理员用）
- [ ] 图片上传功能（接入阿里云OSS或Supabase Storage）
- [ ] 自动触发前端更新

### 第四阶段：评分活数据

**现在的问题：** 评分写死（4.6/1238条），不会自动更新

**解决方案A（近期）：** 定时抓取携程/美团评分

```
定时任务每天跑一次
  ↓ 抓取携程/美团/大众点评评分数字
  ↓ 存入 Supabase
  ↓ 前端读取最新评分
```

**解决方案B（长期）：** 自有评价系统

```
用户订房 → 住完收微信提醒 → 提交图文评价
  ↓ 审核通过 → 显示在页面
  ↓ 评分自动重新计算
```

**需要做的：**
- [ ] 用户评价提交界面（手机端友好）
- [ ] 评价审核后台
- [ ] 评分自动计算逻辑
- [ ] 「住客说」标签自动提取（接入DeepSeek API分析评论）

### 第五阶段：小红书式体验标签

**现在的问题：** 筛选维度是设施标签（海景/早餐/停车），不是用户真实体验维度

**目标筛选维度：**

| 现在 | 未来要加的 |
|------|-----------|
| 海景客房 | 适合情侣 |
| 含早餐 | 拍照出片 |
| 免费停车 | 安静不吵 |
| 游泳池 | 性价比王 |
| | 适合亲子 |
| | 服务态度好 |
| | 适合度假放空 |

**技术方案：**
- 抓取携程/美团/大众点评评论
- 接入 Claude API 做情感分析 + 标签提取
- 自动生成体验标签，存入数据库
- 定期更新

**需要做的：**
- [ ] search.html 筛选条加入体验标签维度
- [ ] DeepSeek API 批量分析评论脚本
- [ ] Claude API 生成标签文案（用户可见部分）
- [ ] 数据库增加 experience_tags 字段

### 第六阶段：用户系统 + 支付

- [ ] 微信登录（OAuth）
- [ ] 用户中心（我的订单/收藏/评价）
- [ ] 微信支付接入（0.6%手续费）
- [ ] 订单管理系统
- [ ] 会员注册 + 续费逻辑

---

## 六、AI API 策略

| 用途 | 推荐 | 原因 |
|------|------|------|
| 批量抓取+分析评论 | DeepSeek API | 便宜（Claude的1/20），量大 |
| 生成展示文案 | Claude API | 中文质量高，输出稳定 |
| 用户对话/推荐 | Claude API | 语境理解准确 |
| 实时搜索补充 | Gemini | 有Google搜索实时数据 |

**接入方式：** 通过香港/新加坡服务器中转，用户访问的是自由网，不直接调API，无被墙问题

---

## 七、省市数据结构（当前）

```javascript
const PROVINCE_CITIES = {
  sd: ['威海','青岛','烟台','泰安','济南','临沂','潍坊','淄博'],
  bj: ['北京'],
  sh: ['上海'],
  gd: ['广州','深圳','珠海','佛山'],
  zj: ['杭州','宁波','温州','嘉兴']
}
```

**扩张方式：** 加一行省份数据 + 一个 `<option>` 即可，无需改其他代码

---

## 八、上线路径

```
1. 申请域名 → ziyouwang.cn（阿里云万网，约¥55/年）
2. 买服务器 → 阿里云轻量应用服务器（约¥80/月）
3. ICP备案（必须，约7-20工作日）
4. 上传文件（3个HTML + 图片资源）
5. 绑定域名
6. 高德地图Key填入 search.html 第一行
```

---

## 九、待接收的截图（用于制作detail页）

> 请对每家酒店提供携程页面截图，包含：

- [ ] 心海海景酒店：酒店主图、评分、房型列表（名称/面积/价格）、设施、评价
- [ ] 汉庭威海火车站：同上
- [ ] 如家·neo高铁北站：同上

---

*文档由 Claude 生成并维护，每次重大更新后同步更新本文档*
